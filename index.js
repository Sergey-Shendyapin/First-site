const button = document.querySelector('button');
const output = document.querySelector('.output');

button.addEventListener('click', function() {
    output.innerHTML = 'Тут могла быть ваша реклама, но её нет :)';
});